using UnityEngine;

public class Sion : MonoBehaviour
{
	// Start is called before the first frame update
	void Start()
	{
		Debug.Log("Hello Unity!");
	}

	// Update is called once per frame
	void Update()
	{

	}
}
